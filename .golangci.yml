version: "2"

run:
  timeout: 5m

linters:
  enable:
    - errcheck
    - govet
    - staticcheck
    - unused
    - gosimple
    - ineffassign
    - shadow
    - gofmt
    - goimports

linters-settings:
  govet:
    enable-all: true

issues:
  exclude-rules:
    - path: _test\.go$
      linters:
        - errcheck
      text: "Error return value"